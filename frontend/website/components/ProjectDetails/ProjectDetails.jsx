const ProjectDetails = ({ project }) => {
    return (
        project && (
            <div className={`w-11/12 h-full flex flex-col items-center py-4 px-1 `}>
                <div className={`w-full h-full flex flex-col items-center gap-y-4 bg-black/10 backdrop-blur-md rounded-md`}>
                    
                </div>
            </div>
        )
    )
}

export default ProjectDetails;